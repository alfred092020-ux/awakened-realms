export const LOGRES_TUTORIAL_POINTER_PRESENTATION =
  Object.freeze({
    source:
      'CONFIRMED_CURRENT_JP_NATIVE_SOURCE_BYTES',
    globalBehavior:
      'CONFIRMED_GLOBAL_VIDEO_HAND_PROMPT',
    globalArtworkApplication:
      'SUPPORTED_INFERENCE',
    placement:
      'RECONSTRUCTED',
  } as const)

export const LOGRES_TUTORIAL_GREEN_JELL_PRESENTATION =
  Object.freeze({
    currentJpResourceFamily:
      'enm_001_000_000',
    source:
      'CONFIRMED_CURRENT_JP_NATIVE_SOURCE_BYTES',
    globalBehavior:
      'CONFIRMED_GLOBAL_GREEN_JELL_TUTORIAL',
    historicalGlobalInternalId:
      'UNRESOLVED',
    globalVisualApplication:
      'SUPPORTED_INFERENCE',
  } as const)

export const LOGRES_TUTORIAL_GREEN_JELL_IDLE_ANIMATION =
  Object.freeze({
    frameCount:
      4,
    frameRate:
      6,
    repeat:
      -1,
  } as const)

export const LOGRES_TUTORIAL_POINTER_OFFSET =
  Object.freeze({
    x:
      0,
    y:
      -36,
    scale:
      0.34,
    placement:
      'RECONSTRUCTED',
  } as const)
